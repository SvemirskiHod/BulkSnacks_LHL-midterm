<!DOCTYPE html>
<html lang="en">
  <head>
    <% include partials/_head%>
  </head>

  <body>
    <nav class="navbar navbar-inverse navbar-fixed-top">



      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar line"></span>
            <span class="icon-bar line"></span>
            <span class="icon-bar line"></span>
          </button>
          <a class="navbar-brand" href="/">BulkSnacks</a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
          <% if (name) {%>
          <% include partials/_logout_form %>
          <% } else if (errors.badpass){ %>
          <% include partials/_password_err %>
          <% } else if (errors.baduser) { %>
          <% include partials/_uname_err %>
          <% } else { %>
          <% include partials/_login_form %>
          <% }; %>
        </div><!--/.navbar-collapse -->
      </div>
    </nav>
    <div class="jumbotron">
      <div class="container">

        <% if (admin) { %>

        <% include partials/_orders_auth %>

        <% } else { %>

        <% include partials/_get_out %>

        <% }; %>

      </div>
    </div>
    <div class="admin-view container">
      <% if (admin) { %>
      <% include partials/_admin_view %>
      <% }; %>

      <% include partials/_footer %>
    </div> <!-- /container -->

    <% include partials/_scripts %>

  </body>
</html>
